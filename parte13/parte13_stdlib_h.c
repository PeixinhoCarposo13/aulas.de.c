//stdlib.h

/*
A biblioteca stdlib.h em C significa Standard Library (Biblioteca Padrão Geral). 
Ela fornece funções úteis para memória, conversões, controle do programa 
e utilidades diversas.
*/

/*
-Alocação dinâmica de memória
malloc() → aloca memória
calloc() → aloca e inicializa com zero
realloc() → redimensiona memória
free() → libera memória
*/

/*
-Conversões de tipos
atoi() → string → int
atof() → string → float
strtol() → string → long
*/

/*
-Números aleatórios
rand() → gera número aleatório
srand() → define semente para rand()
*/

/*
Controle do programa
exit() → termina o programa
abort() → termina com falha
*/

/*
-Ordenação e busca
qsort() → ordena um array
bsearch() → busca em array ordenado
*/

#include <stdio.h>

int main()
{

  return 0;
}